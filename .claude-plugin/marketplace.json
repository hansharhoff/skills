{
  "name": "hansharhoff-skills",
  "description": "Personal Claude Code skills marketplace by Hans Harhoff Andersen",
  "owner": {
    "name": "Hans Harhoff Andersen"
  },
  "plugins": [
    {
      "name": "teach-tech",
      "source": "./plugins/teach-tech",
      "description": "Interactive technology teaching sessions — guides you through new technologies with explanations, diagrams, and hands-on exercises"
    },
    {
      "name": "repo-cleanup",
      "source": "./plugins/repo-cleanup",
      "description": "Audit and clean up a repository: find typos, inconsistencies, outdated references, dead code, failing tests, type errors, linter issues, and documentation gaps"
    },
    {
      "name": "spec-driven-workflow",
      "source": "./plugins/spec-driven-workflow",
      "description": "Iterate code against a spec maintained in a separate location. Snapshot-based diff drives each iteration's work plan; bookkeeping (status, blocked items, happy path) lives in the code repo's spec/working folder."
    },
    {
      "name": "tailscale-served-dashboard",
      "source": "./plugins/tailscale-served-dashboard",
      "description": "Turn a locally-running web app or dashboard on a Mac into a durable, tailnet-reachable service: bind 127.0.0.1, front it with tailscale serve (HTTPS), and supervise it with a launchd LaunchDaemon. Includes scaffold.sh to generate the run script, daemon plist, and installer."
    },
    {
      "name": "whats-next",
      "source": "./plugins/whats-next",
      "description": "PM-style triage + capture for the active workspace. READ mode renders a ranked dashboard of open PRs, in-session tasks, scheduled memory items, Atlassian (when MCP available), git state, and circle-back notes — ≤ 80%-of-screen, ending with a one-line action prompt. CAPTURE mode catches phrases like 'remind me to' or 'let's circle back' and routes them to a task or a dated memory file."
    },
    {
      "name": "agent-team-workflow",
      "source": "./plugins/agent-team-workflow",
      "description": "Run sub-agents alongside a warm main agent so the human's questions aren't blocked. Codifies the in-session strategy (when to dispatch vs do work in main) AND the mechanics (pre-dispatch git-status check, explicit branch convention, return contract, post-merge cleanup) for sub-Agent calls with isolation: 'worktree'. Avoids locked worktrees, uncommitted carry-over, and the 'agent reported completed but the CLI is still running' trap. Scope: a single Claude Code session."
    }
  ]
}
